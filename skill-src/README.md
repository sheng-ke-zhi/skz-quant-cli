# Skill authoring source

Edit `books/` and `common/`, then run:

```bash
python3 scripts/release/build_skills.py --sync-only
```

The command renders all harness-specific skill trees and rebuilds `skills/manifest.json`.
